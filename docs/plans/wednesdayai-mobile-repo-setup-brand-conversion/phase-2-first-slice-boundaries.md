# Phase 2 - First Slice Boundaries

Plan the first safe conversion slice. This phase separates repository/docs metadata, package/workspace names, visible app identity, native identifiers, CI, release, signing, and store metadata.

Done means the first behaviour-changing workstream can be small and can request confirmation only for the final identifiers that actually need a human decision.
