---
id: "002"
phase: 2
title: Update mobile app visible identity copy
status: ready
depends_on: ["001"]
parallel: false
conflicts_with: []
files:
  - apps/mobile/app.json
irreversible: false
scope_test: "N/A"
allowed_change: edit
covers_criteria: [SC3, SC4, SC5]
---
## Failing test (write first)
N/A - app configuration copy edit with manual verification.

## Change
- **File:** `apps/mobile/app.json`
- **Anchor:** `expo.name` near the top of the file.
- **Before:**
```json
    "name": "Clawket",
```
- **After:**
```json
    "name": "WednesdayAI",
```

- **File:** `apps/mobile/app.json`
- **Anchor:** iOS `infoPlist` permission descriptions.
- **Before:**
```json
        "NSCameraUsageDescription": "Allow Clawket to use your camera to scan QR codes and take photos to send to AI.",
        "NSPhotoLibraryAddUsageDescription": "Allow Clawket to save stats posters to your photo library.",
        "NSSpeechRecognitionUsageDescription": "Allow Clawket to transcribe your speech into chat messages.",
```
- **After:**
```json
        "NSCameraUsageDescription": "Allow WednesdayAI to use your camera to scan QR codes and take photos to send to AI.",
        "NSPhotoLibraryAddUsageDescription": "Allow WednesdayAI to save stats posters to your photo library.",
        "NSSpeechRecognitionUsageDescription": "Allow WednesdayAI to transcribe your speech into chat messages.",
```

- **File:** `apps/mobile/app.json`
- **Anchor:** `expo-location` plugin permission copy.
- **Before:**
```json
          "locationAlwaysAndWhenInUsePermission": "Allow Clawket to access your location so your AI agent can know where you are."
```
- **After:**
```json
          "locationAlwaysAndWhenInUsePermission": "Allow WednesdayAI to access your location so your AI agent can know where you are."
```

- **File:** `apps/mobile/app.json`
- **Anchor:** `expo-media-library` plugin permission copy.
- **Before:**
```json
          "photosPermission": "Allow Clawket to save stats posters to your photo library.",
          "savePhotosPermission": "Allow Clawket to save stats posters to your photo library."
```
- **After:**
```json
          "photosPermission": "Allow WednesdayAI to save stats posters to your photo library.",
          "savePhotosPermission": "Allow WednesdayAI to save stats posters to your photo library."
```

## Allowed moves
- Edit only `expo.name` and the permission-copy strings listed above.
- Keep `slug`, `bundleIdentifier`, Android `package`, `scheme`, share extension bundle identifier, app group, `projectId`, `owner`, `appleTeamId`, asset paths, package names, and store identifiers unchanged.
- Do not edit React Native locale files in this task. SC4 is satisfied by not touching locale-backed strings; if a user-facing locale string appears necessary, STOP and split a separate six-locale task.
- Do not edit native project files generated by Expo prebuild.

## STOP triggers
- Any final native identifier, app group, scheme, slug, owner, EAS project ID, Apple team, package name, asset path, icon, splash, screenshot, or store metadata would need to change.
- A changed string is user-facing React Native source or locale copy rather than app config copy.
- `npm run mobile:config:check` reports a failure unrelated to missing local secrets or expected local environment placeholders.

## Manual verification (record in decisions-ledger)
- `node -e "const app=require('./apps/mobile/app.json').expo; if (app.name !== 'WednesdayAI') process.exit(1); if (app.slug !== 'clawket') process.exit(2); if (app.ios.bundleIdentifier !== 'com.expansionx.clawket') process.exit(3); if (app.android.package !== 'com.expansionx.clawket') process.exit(4); if (app.scheme !== 'clawket') process.exit(5); if (app.owner !== 'p697') process.exit(6);"` exits 0.
- `rg -n "Allow Clawket|\\\"name\\\": \\\"Clawket\\\"" apps/mobile/app.json` returns no matches.
- `rg -n "com\\.expansionx\\.clawket|\\\"slug\\\": \\\"clawket\\\"|\\\"scheme\\\": \\\"clawket\\\"|\\\"owner\\\": \\\"p697\\\"|972e845f-da81-44db-a908-24be4ca80288" apps/mobile/app.json` returns matches, proving confirmation-bound identifiers stayed unchanged.
- `npm run mobile:config:check` exits 0, or any failure is recorded with exact output and confirmed unrelated to the copy-only app config edit.

## Done when
`WAI_TYPECHECK_CMD=":" WAI_TEST_CMD=":" bash ~/.claude/wai/scripts/task-gate.sh wednesdayai-mobile-brand-conversion-implementation 002` exits 0 after the manual verification checks above are recorded in `docs/plans/wednesdayai-mobile-brand-conversion-implementation/decisions-ledger.md`.
