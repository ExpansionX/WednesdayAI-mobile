# Phase 2: Fork Setup Contract

Goal: make the hard fork operationally clear before any repository import or brand conversion task runs.

Tasks:

- `003-create-setup.md` creates `SETUP.md`.
- `004-create-forking.md` creates `FORKING.md`.

Exit criteria:

- The setup guide covers repository import, history, identifiers, app display metadata, signing, CI, release metadata, and secrets.
- The fork guide explains how other builders can take the app base and customise it without taking WednesdayAI core.
