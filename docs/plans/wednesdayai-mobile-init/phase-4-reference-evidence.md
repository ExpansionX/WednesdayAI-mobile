# Phase 4: Reference Evidence

Goal: preserve the useful ideas from Clawket, upstream OpenClaw, WednesdayAI core, Saturday, Hermes Desktop, ArgentOS, and other reference systems so the roadmap has traceable source material.

Tasks:

- `008-create-reference-systems-doc.md` creates `docs/architecture/reference-systems.md`.

Exit criteria:

- Each source system has a concise "borrow / avoid / cherry-pick order" note.
- The document supports the roadmap without becoming an implementation task itself.
